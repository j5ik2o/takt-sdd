# プロダクト概要

takt-sdd は、TAKT を基盤に構築された Kiro 互換の仕様駆動開発（Spec-Driven Development）ワークフロー基盤です。宣言的な YAML ワークフロー、ファセット化されたプロンプト、検証ゲート、インストーラー連携をひとまとめにパッケージ化しており、要件定義・設計・タスク分解・実装・レビュー・検証の各工程を、自由形式のエージェントセッションではなく、制御されたステートマシン実行として進められるようにします。

## 主な機能

- SDD の各フェーズを定義する宣言的な TAKT ワークフロー。公開 surface は Kiro 互換の `kiro-*` workflow と `kiro:*` エントリポイントのみです（v2.0.0 で旧 `cc-sdd-*` / `opsx-*` は退役。opsx は将来の再提供を予定）。
- ペルソナ（persona）・ポリシー（policy）・インストラクション（instruction）・ナレッジ（knowledge）・出力契約（output contract）の各ファイルに分割された、ファセット化プロンプト資産。
- `.kiro/specs/<feature>/` 配下の成果物による、Kiro 互換の仕様ワークスペースのサポート。
- `takt-sdd` グローバル CLI（`takt-sdd init` / `takt-sdd <kiro-command>` / `takt-sdd run <workflow>`）。退役 command は系統別の案内を表示して非 0 終了します。
- 既存プロジェクトのスクリプトを上書きせずにスクリプトをマージし、`.takt/` ワークフロー資産をインストールするインストーラー機能。v1.x 導入済みプロジェクトの update では、未変更の退役資産を安全に後始末します（カスタマイズ済みは警告して残置）。

## 想定ユースケース

- Kiro スタイルの仕様成果物を維持しつつ、SDD のためにエージェントワークフローを決定論的にルーティングしたいチーム。
- v1.x（`cc-sdd:*` / `opsx:*` 時代）の導入プロジェクトを、update で v2.0.0 の kiro-* 専用 surface へ移行するプロジェクト。
- TAKT のワークフロー／ファセットのバンドルを、インストーラー経由で公開する前に開発・検証するメンテナー。

## バリュープロポジション

takt-sdd は、エージェントの制御プレーンを明示的にします。AI の出力は確率的なままですが、ワークフローの順序、遷移条件、レビューゲート、成果物の契約は、テスト可能でバージョン管理できる再利用可能な TAKT 資産としてエンコードされます。

---
_網羅的な機能一覧ではなく、プロダクトの目的と意思決定を左右する機能に焦点を当てること。_
_updated_at: 2026-06-11 — retire-legacy-workflow-surface（v2.0.0）の退役を反映: cc-sdd-*/opsx-* の存続記述を是正し、グローバル CLI と update 後始末を追記。_
