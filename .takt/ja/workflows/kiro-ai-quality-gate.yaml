name: kiro-ai-quality-gate
description: Kiro - 完了した 1 実装タスク向けの callable AI アンチパターン品質ゲート
subworkflow:
  callable: true
  visibility: internal
  returns:
    - need_replan
  params:
    fix_instruction:
      type: facet_ref
      facet_kind: instruction
      default: kiro-ai-antipattern-fix-implementation
    domain_knowledge:
      type: facet_ref[]
      facet_kind: knowledge
      default:
        - architecture
workflow_config:
  provider_options:
    codex:
      network_access: true
    opencode:
      network_access: true
max_steps: 12
initial_step: ai-antipattern-review-1st

instructions:
  ai-antipattern-review: ../facets/instructions/ai-review.md
  kiro-ai-antipattern-fix-implementation: ../facets/instructions/kiro-ai-antipattern-fix-implementation.md

policies:
  kiro-artifact-operations: ../facets/policies/kiro-artifact-operations.md
  kiro-impl-task-progress: ../facets/policies/kiro-impl-task-progress.md

report_formats:
  kiro-ai-antipattern-fix-result: ../facets/output-contracts/kiro-ai-antipattern-fix-result.md

loop_monitors:
  - cycle:
      - ai-antipattern-review-1st
      - ai-antipattern-fix
    threshold: 3
    judge:
      persona: supervisor
      instruction: loop-monitor-ai-antipattern-fix
      rules:
        - condition: Healthy (findings are shrinking or the latest fix has concrete finding_decisions)
          next: ai-antipattern-review-1st
        - condition: Unproductive (loop_monitors.threshold reached for ai-antipattern-review-1st and ai-antipattern-fix)
          next: request-replan

steps:
  - name: ai-antipattern-review-1st
    edit: false
    persona: ai-antipattern-reviewer
    policy:
      - review
      - ai-antipattern
      - kiro-artifact-operations
      - kiro-impl-task-progress
    knowledge:
      $param: domain_knowledge
    provider_options:
      claude:
        allowed_tools:
          - Read
          - Glob
          - Grep
          - Bash
    required_permission_mode: readonly
    instruction: ai-antipattern-review
    output_contracts:
      report:
        - name: kiro-ai-antipattern-review.md
          format: ai-antipattern-review
    rules:
      - condition: No AI-specific issues
        next: COMPLETE
      - condition: AI-specific issues found
        next: ai-antipattern-fix
      - when: "true"
        next: request-replan
        appendix: Treat ambiguous, blocked, or internally inconsistent review outcomes as requiring replanning.

  - name: ai-antipattern-fix
    edit: true
    persona: coder
    policy:
      - coding
      - testing
      - ai-antipattern
      - kiro-artifact-operations
      - kiro-impl-task-progress
    session: refresh
    knowledge:
      $param: domain_knowledge
    provider_options:
      claude:
        allowed_tools:
          - Read
          - Glob
          - Grep
          - Edit
          - Write
          - Bash
    required_permission_mode: edit
    pass_previous_response: false
    instruction:
      $param: fix_instruction
    output_contracts:
      report:
        - name: kiro-ai-antipattern-fix.md
          format: kiro-ai-antipattern-fix-result
    rules:
      - condition: STATUS FIXED
        next: ai-antipattern-review-1st
      - condition: STATUS NO_FIX_NEEDED and finding-level evidence supports every finding_decision
        next: COMPLETE
      - condition: STATUS NEED_REPLAN
        return: need_replan
      - condition: STATUS BLOCKED
        next: ABORT

  - name: request-replan
    edit: false
    persona: supervisor
    required_permission_mode: readonly
    instruction: |
      AI アンチパターン品質ゲートで、review outcome が曖昧、または review/fix ループが収束しないため再計画が必要。
      親の実装ワークフローが debug-task に戻れるように need_replan を返す。
    rules:
      - condition: Replan is required after an ambiguous AI antipattern review outcome or an unproductive AI antipattern fix loop
        return: need_replan
